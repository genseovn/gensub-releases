# GenSub releases

Public release assets for the GenSub desktop app.

- Installers and minisign signatures are attached to each GitHub Release.
- `latest.json` is the fallback update manifest used by the desktop client when `server.gensub.vn` is unreachable.

Do not edit these files manually; they are written by `scripts/release-app.ps1`.
